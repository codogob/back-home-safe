image: node:4.2.2

pages:
  cache:
    paths:
    - node_modules/

  script:
  - npm update
  - npm install
  - npm run build
  - mv ./build ./public
  artifacts:
    paths:
    - public
  only:
  - master
